<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.com.zjol">

    <application
        android:name=".ZjolApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="zjol.com.cn.launcher.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="ZBP_APP_ID"
            android:value="1" />
        <meta-data
            android:name="MW_APPID"
            android:value="B5CYHNBKB3SSNGJXX10D2UJVJAU7G3T8" />
        <meta-data
            android:name="SHWANALYTICS_APPKEY"
            android:value="zm1027-002" />
        <meta-data
            android:name="SHWANALYTICS_CHANNEL"
            android:value="yyb" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

    </application>

</manifest>